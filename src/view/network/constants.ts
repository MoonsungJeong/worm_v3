// file for game variables and constants
export const screenWidth: number = window.innerWidth; //document.documentElement.clientWidth;
export const screenHeight: number = window.innerHeight; //document.documentElement.clientHeight;
export const gameSize: number = 3000;
export const gridUnit: number = 50;
export const scale: number = 7;
